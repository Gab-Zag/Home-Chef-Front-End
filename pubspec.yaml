name: home_chef_front_end
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  hive_test: ^1.0.1
  http_mock_adapter: ^0.6.0
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true